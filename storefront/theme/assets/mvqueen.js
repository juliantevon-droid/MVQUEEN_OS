document.addEventListener('DOMContentLoaded',()=>{const button=document.querySelector('[data-mvq-menu]'),panel=document.querySelector('[data-mvq-panel]');if(button&&panel){const setMenu=open=>{if(open)panel.removeAttribute('hidden');else panel.setAttribute('hidden','');button.setAttribute('aria-expanded',String(open));button.setAttribute('aria-label',open?'Close menu':'Open menu');document.documentElement.classList.toggle('mvq-menu-open',open)};button.addEventListener('click',()=>setMenu(panel.hasAttribute('hidden')));panel.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>setMenu(false)));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!panel.hasAttribute('hidden')){setMenu(false);button.focus()}});window.addEventListener('resize',()=>{if(window.innerWidth>=750&&!panel.hasAttribute('hidden'))setMenu(false)},{passive:true})}document.querySelectorAll('a[href^="#"]').forEach(link=>link.addEventListener('click',event=>{const target=document.querySelector(link.getAttribute('href'));if(!target)return;event.preventDefault();target.scrollIntoView({behavior:window.matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'})}))});