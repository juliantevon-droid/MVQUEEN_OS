{% liquid
  assign card_image = product.featured_image
  assign second_image = product.images[1]
  assign first_variant = product.selected_or_first_available_variant
  assign product_label = product.title | escape
  assign has_sale = false
  assign is_jewelry = false
  if product.tags contains 'mvq:category:jewelry'
    assign is_jewelry = true
  endif
  if first_variant.available and first_variant.compare_at_price > first_variant.price
    assign has_sale = true
  endif
%}
<article class="mvq-card{% if is_jewelry %} mvq-card--jewelry{% endif %}">
  <a class="mvq-card-link" href="{{ product.url }}" aria-label="View {{ product_label }}">
    <div class="mvq-card-media">
      {% if card_image %}
        {{ card_image | image_url: width: 900 | image_tag: loading: 'lazy', widths: '360,540,720,900', sizes: '(max-width: 480px) 100vw, (max-width: 800px) 50vw, 33vw', alt: card_image.alt | default: product.title | escape }}
        {% if second_image %}
          <span class="mvq-card-media-alt" aria-hidden="true">
            {{ second_image | image_url: width: 900 | image_tag: loading: 'lazy', widths: '360,540,720,900', sizes: '(max-width: 480px) 100vw, (max-width: 800px) 50vw, 33vw', alt: '' }}
          </span>
        {% endif %}
      {% else %}
        <div class="mvq-card-media-placeholder" aria-hidden="true"></div>
      {% endif %}
      {% unless product.available %}<span class="mvq-badge">Sold out</span>{% endunless %}
      {% if has_sale %}<span class="mvq-badge">Sale</span>{% endif %}
    </div>
    <div class="mvq-card-info">
      {% if product.type != blank %}<p class="mvq-card-type">{{ product.type }}</p>{% endif %}
      <h3 class="mvq-card-title">{{ product.title }}</h3>
      <div class="mvq-card-price">
        {% if has_sale %}
          <span class="mvq-price-current">{{ first_variant.price | money }}</span>
          <s>{{ first_variant.compare_at_price | money }}</s>
        {% else %}
          {{ first_variant.price | money }}
        {% endif %}
      </div>
    </div>
  </a>
</article>